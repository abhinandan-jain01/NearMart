specVersion: "v1"
serviceId: "nearmart-backend"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "nearmart-backend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
